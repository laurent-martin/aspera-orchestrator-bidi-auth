return {
    base_url = "$orch_url",
    username = "$orch_user",
    password = "$orch_pass",
    workflow = $orch_workflow,
    debug = true,
    node_url = "$node_url",
    node_user = "$node_user",
    node_pass = "$node_pass",
}
